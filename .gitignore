# ─── Data (large, local-only) ────────────────────────────────────────────────
2026 Munich Tech Arena - Datas/
2026 Munich Tech Arena - Datas.zip
# Only ignore the large data subdirectories, NOT data/splits/ (must be committed)
data/mesh/
data/landmarks/

# ─── Python temporary files ──────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.ipynb_checkpoints/
.venv/
venv/

# ─── Model weights (large binaries, local-only) ─────────────────────────────
models/

# ─── Run artifacts (large, local-only) ──────────────────────────────────────
# Weights and generated plots/CSVs are local-only.
# Metadata, configs, and summaries ARE committed (small, shared ledger).
runs/*/weights/
runs/*/results/*.csv
runs/*/results/*.png
runs/*/logs/

# ─── Legacy directories ─────────────────────────────────────────────────────
experiments/
output/
splits/

# ─── Distribution / packaging ───────────────────────────────────────────────
dist/
*.zip

# ─── Scratch folder (temporary scripts) ─────────────────────────────────────
scratch/

# ─── IDE configurations ─────────────────────────────────────────────────────
.vscode/
.idea/
.gemini/

# ─── Subprojects ────────────────────────────────────────────────────────────
LeaderBoard/

# ─── COMMITTED (do NOT gitignore these): ────────────────────────────────────
# configs/**
# data/splits/*.txt
# runs/index.csv
# runs/*/config.resolved.yaml
# runs/*/metadata.json
# runs/*/results/summary_*.json
